// write a function that takes a array as the param and return
// the size of the array

function length(arr) {
    return arr.length;
}


let arr = [1, 2, 3, "majid", 'blue'];
let result = length(arr);
console.log(result);

